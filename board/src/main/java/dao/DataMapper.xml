<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dev">
	<select id="selectDataAll" resultType="vo.BoardVO" parameterType="String">
		SELECT idx, board_title, board_contents, board_writer, board_hits FROM board
	</select> 
	
	<select id="selectOne" resultType="vo.BoardVO" parameterType="String">
		SELECT idx, board_title, board_contents, board_writer, board_hits FROM board WHERE idx=#{idx}
	</select>
	
	<select id="detailHitsUpdate" resultType="vo.BoardVO" parameterType="String">
		UPDATE board SET board_hits = board_hits+1 WHERE idx=#{idx}
	</select>
</mapper>